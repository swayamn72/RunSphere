# Copy to .env for local development or provide this at build time.
# Do not commit a preview or deployment URL; public preview tunnels are ephemeral.
EXPO_PUBLIC_API_BASE_URL=https://api.example.com
